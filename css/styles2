:root{--lagoon:#31c3c0;
      --deep:#086c78;
      --navy:#124a55;
      --foam:#f7fff5;
      --sand:#f4dfb3;
      --sand2:#fff4d7;
      --leaf:#5e9b45;
      --leaf2:#2f713f;
      --coral:#e98568;
      --stone:#5b625d;
      --ink:#20464d;
      --muted:#637b7c;
      --white:#fffdf8;
      --line:rgba(32,70,77,.13);
      --shadow:0 20px 50px rgba(16,75,82,.16);
      --radius:30px}

*{box-sizing:border-box;margin:0;padding:0}
html{scroll-behavior:smooth}
body{font-family:Georgia,"Times New Roman",serif;color:var(--ink);background:var(--sand2);line-height:1.7}
a{text-decoration:none;color:inherit}
.container{width:min(92%,1180px);margin:auto}

header{position:sticky;top:0;z-index:50;background:rgba(255,253,248,.9);backdrop-filter:blur(14px);border-bottom:1px solid var(--line)}
nav{min-height:76px;display:flex;align-items:center;justify-content:space-between}
.brand{display:flex;align-items:center;gap:10px;color:var(--deep);font-weight:bold;letter-spacing:.08em;text-transform:uppercase}
.brand-mark{width:42px;height:42px;border-radius:50%;display:grid;place-items:center;background:var(--lagoon);color:#fff}
.nav{display:flex;gap:5px;list-style:none;font:700 .75rem Arial,sans-serif;text-transform:uppercase;letter-spacing:.07em}
.nav a{padding:10px 14px;border-radius:999px}
.nav a:hover{background:#d9f1e7;color:var(--deep)}
.nav a.active{
    background:#d9f1e7;
    color:var(--deep);
    border-radius:999px;
}


/* =========================================
   SITE NAVIGATION
   ========================================= */

.site-header {
    width: 100%;
    background: var(--navy);
    border-bottom: 1px solid rgba(255,255,255,0.12);
    position: relative;
    z-index: 100;
}

.site-nav {
    min-height: 76px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
}

.site-nav .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    color: var(--foam);
    text-decoration: none;
    font-weight: 700;
    white-space: nowrap;
}

.site-nav .brand-mark {
    font-size: 1.4rem;
}

.site-nav .nav-links {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 0.35rem;
    margin: 0;
    padding: 0;
    list-style: none;
}

.site-nav .nav-links a {
    display: block;
    padding: 0.65rem 0.85rem;
    color: var(--foam);
    text-decoration: none;
    border-radius: 8px;
    transition:
        background 0.2s ease,
        color 0.2s ease;
}

.site-nav .nav-links a:hover {
    background: rgba(255,255,255,0.10);
}

.site-nav .nav-links a.active {
    background: var(--lagoon);
    color: white;
}


/* =========================================
   HUMAN PAGE HERO
   ========================================= */

.hero-inner {
    display: grid;
    grid-template-columns: minmax(0, 1.1fr) minmax(300px, 0.9fr);
    align-items: center;
    gap: 3rem;
}

.hero-content {
    min-width: 0;
}

.hero-art {
    width: 100%;
    min-width: 0;
}

.hero-art img {
    display: block;
    width: 100%;
    height: 420px;
    object-fit: cover;
    border-radius: 18px;
    box-shadow: 0 12px 35px rgba(0,0,0,0.15);
}


/* =========================================
   MOBILE
   ========================================= */

@media (max-width: 900px) {

    .site-nav {
        flex-direction: column;
        align-items: stretch;
        padding: 1rem 0;
    }

    .site-nav .brand {
        justify-content: center;
    }

    .site-nav .nav-links {
        justify-content: center;
        flex-wrap: wrap;
    }

    .hero-inner {
        grid-template-columns: 1fr;
    }

    .hero-art img {
        height: 320px;
    }

}

@media (max-width: 600px) {

    .site-nav .nav-links {
        gap: 0.15rem;
    }

    .site-nav .nav-links a {
        padding: 0.5rem 0.6rem;
        font-size: 0.9rem;
    }

    .hero-art img {
        height: 250px;
    }

}

/* ------------------------------
   SECTION VARIANTS
------------------------------ */

.section-green {
    background: #edf5e8;
}

.section-mint {
    background: #e5f4ed;
}

.hero-closing {
    padding: 75px 0;
}

.heading-centered {
    margin-inline: auto;
    text-align: center;
}

/* ------------------------------
   BRANCH / HOMELAND LABELS
------------------------------ */

.branch-name {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 10px;
}

.branch-name span {
    padding: 5px 9px;
    border-radius: 999px;
    background: #e4f3ed;
    color: var(--leaf2);
    font-family: Arial, sans-serif;
    font-size: .68rem;
    font-weight: 700;
    letter-spacing: .07em;
    text-transform: uppercase;
}

.footer-copy {
    margin-top: 10px;
}
.hero{position:relative;overflow:hidden;padding:55px 0 90px;background:linear-gradient(180deg,#bdeee8 0%,#d9f0d5 58%,#f4dfb3 100%)}
.hero:after{content:"";position:absolute;left:-5%;right:-5%;bottom:-55px;height:120px;background:var(--sand2);border-radius:50% 50% 0 0/80% 80% 0 0}
.hero-grid{position:relative;z-index:2;min-height:650px;display:grid;grid-template-columns:1fr .9fr;align-items:center;gap:48px}
.eyebrow{display:inline-block;padding:8px 15px;border-radius:999px;background:rgba(255,255,255,.76);color:var(--deep);font:700 .7rem Arial,sans-serif;letter-spacing:.16em;text-transform:uppercase;margin-bottom:20px}h1{font-size:clamp(3.5rem,8vw,7rem);line-height:.87;letter-spacing:-.04em;color:var(--deep);margin-bottom:24px}.hero p{max-width:640px;font-size:1.15rem;color:#28585d;margin-bottom:30px}
.buttons{display:flex;flex-wrap:wrap;gap:12px}
.button{padding:13px 23px;border-radius:999px;border:2px solid var(--deep);font:700 .75rem Arial,sans-serif;text-transform:uppercase;letter-spacing:.07em;transition:.2s}
.button.primary{background:var(--lagoon);border-color:var(--lagoon);color:white;box-shadow:0 9px 22px rgba(49,195,192,.25)}
.button:hover{transform:translateY(-3px);box-shadow:var(--shadow)}

.art{height:620px;border:7px solid rgba(255,255,255,.82);border-radius:46% 46% 34px 34px/24% 24% 34px 34px;overflow:hidden;box-shadow:var(--shadow);position:relative;background:var(--lagoon)}
.art img{width:100%;height:100%;object-fit:cover;object-position:center}
.art:after{content:"";position:absolute;inset:0;background:linear-gradient(180deg,rgba(255,255,255,.03),rgba(5,70,75,.1));pointer-events:none}

.art-caption{position:absolute;z-index:2;left:20px;bottom:20px;padding:13px 16px;border-radius:20px;background:rgba(255,253,248,.88);color:var(--deep);box-shadow:0 8px 22px rgba(0,0,0,.12);font-size:.86rem}
.art-caption strong{display:block}
section{padding:95px 0}
section:nth-of-type(even){background:#e7f4ec}
.kicker{display:inline-block;color:var(--coral);font:700 .72rem Arial,sans-serif;text-transform:uppercase;letter-spacing:.16em;margin-bottom:8px}
h2{font-size:clamp(2.1rem,5vw,3.4rem);line-height:1.05;color:var(--deep);margin-bottom:14px}
.heading{max-width:760px;margin-bottom:42px}
.heading p{color:var(--muted)}
.story{display:grid;grid-template-columns:1fr 1fr;gap:48px;align-items:center}
.story p+p{margin-top:17px}
.postcard{min-height:360px;border-radius:var(--radius);padding:34px;position:relative;overflow:hidden;background:linear-gradient(135deg,#42c7c1 0 43%,#d7efcc 43% 67%,#f1d9a8 67%);box-shadow:var(--shadow)}
.postcard:before,.postcard:after{content:"";position:absolute;border-radius:50%;border:2px dashed rgba(255,255,255,.65)}
.postcard:before{width:300px;height:150px;left:-90px;bottom:35px;transform:rotate(-18deg)}
.postcard:after{width:260px;height:130px;right:-75px;top:30px;transform:rotate(20deg)}
.note{position:relative;z-index:2;max-width:350px;padding:23px;border-radius:23px;background:rgba(255,253,248,.88)}
.note h3{color:var(--deep);margin-bottom:7px}
.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:20px}
.card{background:var(--white);border:1px solid var(--line);border-radius:var(--radius);padding:28px;min-height:210px;box-shadow:0 8px 25px rgba(16,75,82,.06);transition:.22s}
.card:hover{transform:translateY(-5px);box-shadow:var(--shadow)}
.icon{width:49px;height:49px;border-radius:17px;display:grid;place-items:center;background:#d8f0e6;margin-bottom:17px;font-size:1.25rem}
.card h3{color:var(--deep);font-size:1.28rem;margin-bottom:7px}
.card p{color:var(--muted);font-size:.94rem}
.banner{padding:110px 20px;text-align:center;color:white;background:linear-gradient(rgba(5,91,105,.55),rgba(5,91,105,.55)),url('../assets/island-reference.jpg') center 48%/cover}
.banner h2{color:white}
.banner p{max-width:700px;margin:auto;color:#f2fffc}
.timeline{max-width:820px;margin:auto}
.item{position:relative;margin-left:15px;padding:0 0 35px 36px;border-left:2px solid #b8ddd2}
.item:last-child{border-left-color:transparent}

.item:before{content:"";position:absolute;left:-9px;top:2px;width:13px;height:13px;border-radius:50%;background:var(--coral);border:3px solid var(--sand2);box-shadow:0 0 0 2px var(--coral)}
.item strong{display:block;color:var(--coral);margin-bottom:4px}.item p{color:var(--muted)}
footer{padding:45px 0;text-align:center;background:var(--navy);color:#d9eeea}
footer .brand{justify-content:center;color:#fff;margin-bottom:8px}
footer p{font: .8rem Arial,sans-serif;opacity:.85;}
/* =========================================
   MOBILE
   ========================================= */

@media (max-width: 900px) {

    .site-nav {
        flex-direction: column;
        align-items: stretch;

        padding: 1rem 0;
    }

    .site-nav .brand {
        justify-content: center;
    }

    .site-nav .nav-links {
        justify-content: center;
        flex-wrap: wrap;
    }

    .hero-inner {
        grid-template-columns: 1fr;
    }

    .hero-art {
        height: 380px;
    }
}


@media (max-width: 600px) {

    .site-nav .nav-links {
        gap: .15rem;
    }

    .site-nav .nav-links a {
        padding: .5rem .6rem;
        font-size: .7rem;
    }

    .hero {
        padding-top: 45px;
    }

    .hero-art {
        height: 300px;
        border-width: 5px;
        border-radius: 30px;
    }

    h1 {
        font-size: 3.2rem;
    }
}
